import { 
  ResourceManagement, 
  AssetType, 
  UserAuthentication,
  AssetConnectionMethod,
  AccountManagement,
  ACLsManagement,
  AdditionalFeatures,
  AuditManagement,
  OtherFeatures,
} from '@/components/docs/compare-versions-data'
import CompareVersionsDataTable from '@/components/docs/compare-versions-datatable'

# JumpServer CE vs EE

This topic describes the feature comparison between the JumpServer open-source edition (JumpServer CE) and the enterprise edition (JumpServer EE).

<ApplyTrialLicense />

## Comparison Table
This section provides a feature comparison across seven categories.
1. [Resource & Access Management](#resource--access-management)
2. [User Authentication Methods](#user-authentication-methods)
3. [Available Asset Types](#available-asset-types)
4. [Asset Connection Methods](#asset-connection-methods)
5. [Access Control Policies (ACLs)](#access-control-policies-acls)
6. [Audit & Monitoring Capabilities](#audit--monitoring-capabilities)
7. [Additional Features & Integrations](#additional-features--integrations)

## Resource & Access Management

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={ResourceManagement.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## User Authentication Methods

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={UserAuthentication.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## Available Asset Types

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={AssetType.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## Asset Connection Methods

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={AssetConnectionMethod.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## Access Control Policies (ACLs)

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={ACLsManagement.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## Audit & Monitoring Capabilities

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={AuditManagement.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>

## Additional Features & Integrations

<CompareVersionsDataTable
  headers={['Feature', 'JumpServer CE', 'JumpServer EE']}
  rows={OtherFeatures.map((row) => [
    { content: row['feature'] },
    { content: row['ce'] === false ? "❌" : "✅" },
    { content: "✅" },
  ])}
/>