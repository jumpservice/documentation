import { changelogData } from '@/components/docs/changelog'

# Changelogs

<DataTable
  headers={['Version', 'Date', 'Release Note']}
  rows={changelogData.map((row) => [
    { content: row['version'] },
    { content: row['date'] },
    { content: 'Read More', link: `https://github.com/jumpserver/jumpserver/releases/${row.version}` }
  ])}
/>
